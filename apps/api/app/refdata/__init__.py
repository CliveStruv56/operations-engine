"""Platform reference data shared across modules.

Per-module catalogues live with their module (`app.groundwork.seeds`,
`app.grants.seeds`). This package is for reference data no single module
owns — starting with funder question sets, which Groundwork, Grantwork and
later Tenderhouse all draft against.
"""

// Contacts styling, re-exported from the shared Hearth primitives so the
// module keeps its familiar import path without keeping its own definitions.
// New contacts UI should import the components from @/components/ui.
export {
  btnPrimary as btn,
  btnQuiet as btnGhost,
  input,
  label,
} from "@/components/ui/styles";

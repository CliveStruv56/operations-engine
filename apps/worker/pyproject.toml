[project]
name = "ops-engine-worker"
version = "0.1.0"
description = "Operations Engine background worker (Docling parsing + embedding, Slice 3)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "arq>=0.26",
]

[dependency-groups]
dev = ["ruff>=0.6"]

[tool.uv]
package = false
